previewScreenController = function() {
  var that = controller();
    that.setup = function() {
        console.log('Snap into a slim jim and show preview screen');
    };
    that.tearDown = function() {

    };
    return that;
};